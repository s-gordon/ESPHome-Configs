# CI fixture: DETA Grid Connect 2-gang (6912HAMBK), BK72XX / wb3s.
substitutions:
  name: "ci-deta-2-gang-wb3s"
  friendly_name: "CI Deta 2 Gang wb3s"

packages:
  base: !include ../esphome/packages/config/deta/grid-connect-switch/base-bk72xx-wb3s.yaml
  variant: !include ../esphome/packages/config/deta/grid-connect-switch/2-gang-bk72xx-wb3s.yaml

api:
  encryption:
    key: !secret apikey

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
