# CI fixture: DETA Grid Connect 2-gang (6912HAMBK), BK72XX / wb3s.
# This variant file defines its own name/friendly_name substitutions.
packages:
  base: !include ../esphome/packages/config/detagrid-smart-switch/base-BK72XX.yaml
  variant: !include ../esphome/packages/config/detagrid-smart-switch/DETA-Grid-Connect-Smart-Switch-2-gang_BK72XX.yaml

api:
  encryption:
    key: !secret apikey

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
