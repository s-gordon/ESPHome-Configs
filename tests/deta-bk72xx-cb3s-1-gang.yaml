# CI fixture: DETA Grid Connect 1-gang, BK72XX / cb3s.
substitutions:
  name: "ci-deta-1-gang-cb3s"
  friendly_name: "CI Deta 1 Gang cb3s"

packages:
  base: !include ../esphome/packages/config/detagrid-smart-switch/base-BK72XX-cb3s.yaml
  variant: !include ../esphome/packages/config/detagrid-smart-switch/1-gang-cb3s.yaml

api:
  encryption:
    key: !secret apikey

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
